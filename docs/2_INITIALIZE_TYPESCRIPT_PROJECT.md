Task 2: Initialize TypeScript Project

- Initialized npm project with `npm init -y`
- Installed TypeScript as a dev dependency with `npm install typescript --save-dev`
- Created `src/` and `tests/` directories
- Updated README.md with project structure and development information
