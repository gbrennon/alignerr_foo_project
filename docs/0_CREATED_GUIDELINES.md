In this task, I created the file `docs/0_GUIDELINES.md` with the following content:

You should do the following before executing prompt instructions:
- Read all files inside `docs/`
- At the end of the task write a new md file inside `docs/` following with the name `<task_number>_<what_have_you_done>.md` explaining what have u done in this task.
-  The purpose is to feed your context window in the next winddow
- `task_number`  and `what_have_you_done` are placeholders that should be filled
- After writing docs, commit
